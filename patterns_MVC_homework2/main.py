# Задание 2. Создайте класс Рецепт. Необходимо хранить следующую информацию:
# ■ название рецепта;
# ■ автор рецепта;
# ■ тип рецепта (первое, второе блюдо и т.д.);
# ■ текстовое описание рецепта;
# ■ ссылка на видео с рецептом;
# ■ список ингредиентов;
# ■ название кухни (итальянская, французская, украинская и т.д.).
# Создайте необходимые методы для этого класса. Реализуйте паттерн MVC для класса Рецепт и
# код для использования модели, контроллера и представления.

from model import Recipe
from view import RecipeView
from controller import RecipeController

print("Задание №2\n-------------------------------------")

# === USAGE (Использование) ===
if __name__ == "__main__":
    # Создаем рецепт
    borscht = Recipe(
        title="Украинский борщ",
        author="Бабушка Галя",
        recipe_type="Первое блюдо",
        description="Традиционный украинский борщ с капустой, свеклой и говядиной.",
        video_url="https://example.com/borscht-video",
        ingredients=["Свекла", "Капуста", "Картофель", "Морковь", "Говядина", "Лук", "Чеснок", "Соль", "Перец", "Уксус"],
        cuisine="Украинская"
    )

    # Инициализируем представление и контроллер
    view = RecipeView()
    controller = RecipeController(borscht, view)

    # Показываем рецепт
    controller.show_recipe()

    # Вносим изменения
    controller.add_ingredient("Лавровый лист")
    controller.change_description("Борщ — это не просто суп, а душа украинской кухни!")

    # Показываем обновлённый рецепт
    controller.show_recipe()
